# note that this file needs to have the following permissions prior to actual use:
# Access: (0440/-r--r-----)  Uid: (    0/    root)   Gid: (    0/    root)
# that's perms of 440 and ownership by root:root
#
# This file MUST be edited with the 'visudo' command as root.
#
# Please consider adding local content in /etc/sudoers.d/ instead of
# directly modifying this file.
#
# See the man page for details on how to write a sudoers file.
#
Defaults	env_reset,timestamp_type=global
Defaults	mail_badpass
Defaults	secure_path="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin"

# Host alias specification

# User alias specification

# Cmnd alias specification

# User privilege specification
root	ALL=(ALL:ALL) ALL

# Members of the admin group may gain root privileges
%admin ALL=(ALL) ALL

# Allow members of group sudo to execute any command
%sudo	ALL=(ALL:ALL) ALL

# permit users to run certain commands without being prompted for their password
%sudo	ALL=(ALL:ALL)NOPASSWD: /bin/systemctl status $example_service
%sudo	ALL=(ALL:ALL)NOPASSWD: /bin/systemctl start $example_service
%sudo	ALL=(ALL:ALL)NOPASSWD: /bin/systemctl restart $example_service
%sudo	ALL=(ALL:ALL)NOPASSWD: /bin/systemctl stop $example_service

# See sudoers(5) for more information on "#include" directives:

#includedir /etc/sudoers.d
